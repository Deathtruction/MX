{% extends 'base.html.twig' %}

{% block body %}

    <div class="row">
        <div class="col-md-3">
            <div class="new-Membre" style="background-color: #e6e6e6">
                <h1 class="titreG">Modifier Membre</h1>

                {{ form_start(edit_form) }}
                {{ form_widget(edit_form) }}
                <input type="submit" value="Valider la modification"/>
                {{ form_end(edit_form) }}<br>

                <ul>
                    {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                        <li><a href="{{ path('user_show', { 'id': app.user.id }) }}">Retour au Profil</a></li>
                    {% endif %}

                    {#<li>#}
                    {#<a href="{{ path('user_index') }}">Retour à la liste</a>#}
                    {#</li>#}
                    {#<li>#}
                    {#{{ form_start(delete_form) }}#}
                    {#<input type="submit" value="Supprimer">#}
                    {#{{ form_end(delete_form) }}#}
                    {#</li>#}
                </ul>
            </div>
        </div>
    </div>
{% endblock %}
