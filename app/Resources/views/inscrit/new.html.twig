{% extends 'base.html.twig' %}

{% block body %}

    <div class="row">
        <div class="col-md-4">
            <div class="new-inscription">
                <h1 class="titreG">Créer une inscription</h1>
                {{ form_start(form) }}
                {{ form_widget(form) }}
                <input type="submit" value="Créer"/>
                {{ form_end(form) }}

                {#<ul>#}
                {#<li>#}
                {#<a href="{{ path('inscrit_index') }}">Retour à la liste</a>#}
                {#</li>#}
                {#</ul>#}
            </div>
        </div>
    </div>
{% endblock %}
