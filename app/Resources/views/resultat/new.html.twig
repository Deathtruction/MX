{% extends 'base.html.twig' %}

{% block body %}

    <div class="row">
        <div class="col-md-4">
            <div class="new-resultat">
                <h1>Créer un résultat</h1>
                {{ form_start(form) }}
                {{ form_widget(form) }}
                <input type="submit" value="Créer"/>
                {{ form_end(form) }}<br>

                <ul>
                    <li>
                        <a href="{{ path('resultat_index') }}">Retour à la liste</a>
                    </li>
                </ul>
            </div>
        </div>
    </div>
{% endblock %}
